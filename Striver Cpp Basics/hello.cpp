#include<iostream> // It includes a library that contains input output functions
// #include<bits/stdc++.h>  includes all libraries that is present in stdc++ directory which
//                          otherwise may have to be included by using include with specific library name or header..
using namespace std;  // defines that we use standard namespace for i/o...

int main(){
    
    cout << "Hello! I am Kas..";
    return 0;
}